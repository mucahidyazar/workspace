import React from "react";

const drawerToggle = props => (
  <div className="drawer-toggle" onClick={props.opened}>
    <div className="drawer-toggle__logo"></div>
    <div className="drawer-toggle__logo"></div>
    <div className="drawer-toggle__logo"></div>
  </div>
);

export default drawerToggle;
