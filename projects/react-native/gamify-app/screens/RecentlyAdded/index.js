import React from 'react';
import {
  View,
  Text,
  StyleSheet
} from 'react-native';

const RecentlyAdded = props => (
  <View style={styles.container}>
    <Text>RecentlyAdded Screen</Text>
  </View>
);

const styles = StyleSheet.create({
  container: {

  }
});

export default RecentlyAdded;