const INITIAL_STATE = {
  home: {
    version: 1,
  },
  image: {
    url: '',
  },
}

export const HomeReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state
  }
}
